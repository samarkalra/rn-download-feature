export enum EncodingNames {
  utf8 = 'utf8',
  base64 = 'base64',
  ascii = 'ascii',
  uri = 'uri',
}

export enum FileType {
  Image = 'image',
  Pdf = 'pdf',
}

export enum FileExtensions {
  jpg = '.jpg',
  pdf = '.pdf',
}
